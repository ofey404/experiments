-- create "kv_pairs" table
CREATE TABLE "kv_pairs" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "key" character varying NOT NULL, "value" character varying NOT NULL, PRIMARY KEY ("id"));
-- create index "kv_pairs_key_key" to table: "kv_pairs"
CREATE UNIQUE INDEX "kv_pairs_key_key" ON "kv_pairs" ("key");
