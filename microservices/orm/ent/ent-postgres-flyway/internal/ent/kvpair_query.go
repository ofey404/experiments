// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ofey404/experiments/microservices/orm/ent/ent-postgres-flyway/internal/ent/kvpair"
	"github.com/ofey404/experiments/microservices/orm/ent/ent-postgres-flyway/internal/ent/predicate"
)

// KVPairQuery is the builder for querying KVPair entities.
type KVPairQuery struct {
	config
	ctx        *QueryContext
	order      []kvpair.OrderOption
	inters     []Interceptor
	predicates []predicate.KVPair
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the KVPairQuery builder.
func (kpq *KVPairQuery) Where(ps ...predicate.KVPair) *KVPairQuery {
	kpq.predicates = append(kpq.predicates, ps...)
	return kpq
}

// Limit the number of records to be returned by this query.
func (kpq *KVPairQuery) Limit(limit int) *KVPairQuery {
	kpq.ctx.Limit = &limit
	return kpq
}

// Offset to start from.
func (kpq *KVPairQuery) Offset(offset int) *KVPairQuery {
	kpq.ctx.Offset = &offset
	return kpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (kpq *KVPairQuery) Unique(unique bool) *KVPairQuery {
	kpq.ctx.Unique = &unique
	return kpq
}

// Order specifies how the records should be ordered.
func (kpq *KVPairQuery) Order(o ...kvpair.OrderOption) *KVPairQuery {
	kpq.order = append(kpq.order, o...)
	return kpq
}

// First returns the first KVPair entity from the query.
// Returns a *NotFoundError when no KVPair was found.
func (kpq *KVPairQuery) First(ctx context.Context) (*KVPair, error) {
	nodes, err := kpq.Limit(1).All(setContextOp(ctx, kpq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{kvpair.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (kpq *KVPairQuery) FirstX(ctx context.Context) *KVPair {
	node, err := kpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first KVPair ID from the query.
// Returns a *NotFoundError when no KVPair ID was found.
func (kpq *KVPairQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = kpq.Limit(1).IDs(setContextOp(ctx, kpq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{kvpair.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (kpq *KVPairQuery) FirstIDX(ctx context.Context) int {
	id, err := kpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single KVPair entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one KVPair entity is found.
// Returns a *NotFoundError when no KVPair entities are found.
func (kpq *KVPairQuery) Only(ctx context.Context) (*KVPair, error) {
	nodes, err := kpq.Limit(2).All(setContextOp(ctx, kpq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{kvpair.Label}
	default:
		return nil, &NotSingularError{kvpair.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (kpq *KVPairQuery) OnlyX(ctx context.Context) *KVPair {
	node, err := kpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only KVPair ID in the query.
// Returns a *NotSingularError when more than one KVPair ID is found.
// Returns a *NotFoundError when no entities are found.
func (kpq *KVPairQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = kpq.Limit(2).IDs(setContextOp(ctx, kpq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{kvpair.Label}
	default:
		err = &NotSingularError{kvpair.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (kpq *KVPairQuery) OnlyIDX(ctx context.Context) int {
	id, err := kpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of KVPairs.
func (kpq *KVPairQuery) All(ctx context.Context) ([]*KVPair, error) {
	ctx = setContextOp(ctx, kpq.ctx, "All")
	if err := kpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*KVPair, *KVPairQuery]()
	return withInterceptors[[]*KVPair](ctx, kpq, qr, kpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (kpq *KVPairQuery) AllX(ctx context.Context) []*KVPair {
	nodes, err := kpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of KVPair IDs.
func (kpq *KVPairQuery) IDs(ctx context.Context) (ids []int, err error) {
	if kpq.ctx.Unique == nil && kpq.path != nil {
		kpq.Unique(true)
	}
	ctx = setContextOp(ctx, kpq.ctx, "IDs")
	if err = kpq.Select(kvpair.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (kpq *KVPairQuery) IDsX(ctx context.Context) []int {
	ids, err := kpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (kpq *KVPairQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, kpq.ctx, "Count")
	if err := kpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, kpq, querierCount[*KVPairQuery](), kpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (kpq *KVPairQuery) CountX(ctx context.Context) int {
	count, err := kpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (kpq *KVPairQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, kpq.ctx, "Exist")
	switch _, err := kpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (kpq *KVPairQuery) ExistX(ctx context.Context) bool {
	exist, err := kpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the KVPairQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (kpq *KVPairQuery) Clone() *KVPairQuery {
	if kpq == nil {
		return nil
	}
	return &KVPairQuery{
		config:     kpq.config,
		ctx:        kpq.ctx.Clone(),
		order:      append([]kvpair.OrderOption{}, kpq.order...),
		inters:     append([]Interceptor{}, kpq.inters...),
		predicates: append([]predicate.KVPair{}, kpq.predicates...),
		// clone intermediate query.
		sql:  kpq.sql.Clone(),
		path: kpq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Key string `json:"key,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.KVPair.Query().
//		GroupBy(kvpair.FieldKey).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (kpq *KVPairQuery) GroupBy(field string, fields ...string) *KVPairGroupBy {
	kpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &KVPairGroupBy{build: kpq}
	grbuild.flds = &kpq.ctx.Fields
	grbuild.label = kvpair.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Key string `json:"key,omitempty"`
//	}
//
//	client.KVPair.Query().
//		Select(kvpair.FieldKey).
//		Scan(ctx, &v)
func (kpq *KVPairQuery) Select(fields ...string) *KVPairSelect {
	kpq.ctx.Fields = append(kpq.ctx.Fields, fields...)
	sbuild := &KVPairSelect{KVPairQuery: kpq}
	sbuild.label = kvpair.Label
	sbuild.flds, sbuild.scan = &kpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a KVPairSelect configured with the given aggregations.
func (kpq *KVPairQuery) Aggregate(fns ...AggregateFunc) *KVPairSelect {
	return kpq.Select().Aggregate(fns...)
}

func (kpq *KVPairQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range kpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, kpq); err != nil {
				return err
			}
		}
	}
	for _, f := range kpq.ctx.Fields {
		if !kvpair.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if kpq.path != nil {
		prev, err := kpq.path(ctx)
		if err != nil {
			return err
		}
		kpq.sql = prev
	}
	return nil
}

func (kpq *KVPairQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*KVPair, error) {
	var (
		nodes = []*KVPair{}
		_spec = kpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*KVPair).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &KVPair{config: kpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, kpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (kpq *KVPairQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := kpq.querySpec()
	_spec.Node.Columns = kpq.ctx.Fields
	if len(kpq.ctx.Fields) > 0 {
		_spec.Unique = kpq.ctx.Unique != nil && *kpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, kpq.driver, _spec)
}

func (kpq *KVPairQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(kvpair.Table, kvpair.Columns, sqlgraph.NewFieldSpec(kvpair.FieldID, field.TypeInt))
	_spec.From = kpq.sql
	if unique := kpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if kpq.path != nil {
		_spec.Unique = true
	}
	if fields := kpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, kvpair.FieldID)
		for i := range fields {
			if fields[i] != kvpair.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := kpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := kpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := kpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := kpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (kpq *KVPairQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(kpq.driver.Dialect())
	t1 := builder.Table(kvpair.Table)
	columns := kpq.ctx.Fields
	if len(columns) == 0 {
		columns = kvpair.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if kpq.sql != nil {
		selector = kpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if kpq.ctx.Unique != nil && *kpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range kpq.predicates {
		p(selector)
	}
	for _, p := range kpq.order {
		p(selector)
	}
	if offset := kpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := kpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// KVPairGroupBy is the group-by builder for KVPair entities.
type KVPairGroupBy struct {
	selector
	build *KVPairQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (kpgb *KVPairGroupBy) Aggregate(fns ...AggregateFunc) *KVPairGroupBy {
	kpgb.fns = append(kpgb.fns, fns...)
	return kpgb
}

// Scan applies the selector query and scans the result into the given value.
func (kpgb *KVPairGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, kpgb.build.ctx, "GroupBy")
	if err := kpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KVPairQuery, *KVPairGroupBy](ctx, kpgb.build, kpgb, kpgb.build.inters, v)
}

func (kpgb *KVPairGroupBy) sqlScan(ctx context.Context, root *KVPairQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(kpgb.fns))
	for _, fn := range kpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*kpgb.flds)+len(kpgb.fns))
		for _, f := range *kpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*kpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := kpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// KVPairSelect is the builder for selecting fields of KVPair entities.
type KVPairSelect struct {
	*KVPairQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (kps *KVPairSelect) Aggregate(fns ...AggregateFunc) *KVPairSelect {
	kps.fns = append(kps.fns, fns...)
	return kps
}

// Scan applies the selector query and scans the result into the given value.
func (kps *KVPairSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, kps.ctx, "Select")
	if err := kps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KVPairQuery, *KVPairSelect](ctx, kps.KVPairQuery, kps, kps.inters, v)
}

func (kps *KVPairSelect) sqlScan(ctx context.Context, root *KVPairQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(kps.fns))
	for _, fn := range kps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*kps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := kps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
